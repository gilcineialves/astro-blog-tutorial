---
import LayoutBase from "../layouts/LayoutBase.astro";
import "../styles/global.css";
const tituloPagina = "Sobre Mim";

const identidade = {
  nome: "Gilcinei",
  pais: "Alves",
  ocupacao: "Desenvolvedor FullStack",
  hobbies: ["jogar", "caminhar", "programar"],
};

const habilidades = ["HTML", "CSS", "JavaScript", "Vue", "Astro"];

const feliz = true;
const terminado = true;
const meta = 3;
---

<LayoutBase tituloPagina={tituloPagina}>
  <h2>... e meu novo site Astro!</h2>

  <p>
    Estou resolvendo o tutorial introdutório do Astro. Esta é a segunda página
    no meu website, e é a primeira que eu mesmo construí!
  </p>

  <p>
    Este site será atualizado enquanto eu completo mais desse tutorial, então
    continue verificando e veja como minha jornada está indo!
  </p>
  <p>Aqui estão alguns fatos sobre mim:</p>
  <ul>
    <li>Meu nome é {identidade.nome}.</li>
    <li>
      Eu moro no {identidade.pais} e eu trabalho como um {identidade.ocupacao}.
    </li>
    {
      identidade.hobbies.length >= 2 && (
        <li>
          Dois dos meus hobbies são: {identidade.hobbies[0]} and{" "}
          {identidade.hobbies[1]}
        </li>
      )
    }
  </ul>
  <p>Minhas habilidades são:</p>
  <ul>
    {habilidades.map((habilidade) => <li class="habilidade">{habilidade}</li>)}
  </ul>
  {feliz && <p>Estou feliz em estar aprendendo Astro!</p>}

  {terminado && <p>Eu terminei esse tutorial!</p>}

  {
    meta === 3 ? (
      <p>Minha meta é terminar em 3 dias.</p>
    ) : (
      <p>Minha meta não é em 3 dias.</p>
    )
  }
</LayoutBase>
